# D320

D320 embedded instrument cluster project.

This repository contains the source code, bootloader project files, selected documents, fonts, and generated image resources needed for development.

Large binary assets are intentionally not committed, including PSD design sources, supplier installer packages, firmware history outputs, CAN replay archives, and generated build outputs. Keep those files in local/archive storage instead of GitHub.
